# Error handling

Ask via helpers:

- validation vs auth vs rate limit vs provider vs transport
- `delivery_outcome()` / `is_timeout()` / `is_connection_failure()`
- `retry_after()` / `request_id()` / `provider_code()`

Never log Authorization, client secrets, webhook secrets, or raw PAN/PII.
