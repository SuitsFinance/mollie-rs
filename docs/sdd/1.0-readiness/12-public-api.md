# SDD 12 — Public API

Tier S stable intent; Tier G may churn with OpenAPI pin pre-1.0.
Document in `docs/API-STABILITY.md` (Phase 7).
Facade naming: `payments()`, `payouts()`, `transfers()`, …
Builders only when preventing invalid states.
