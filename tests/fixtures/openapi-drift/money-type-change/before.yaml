openapi: 3.0.3
info: {title: t, version: '1'}
paths: {}
components:
  schemas:
    Payment:
      type: object
      properties:
        amount:
          type: object
          properties:
            value: {type: string}
            currency: {type: string}
