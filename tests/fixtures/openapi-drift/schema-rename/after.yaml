openapi: 3.0.3
info: {title: t, version: '1'}
paths: {}
components:
  schemas:
    ShippingAddress:
      type: object
      properties:
        streetAndNumber: {type: string}
        city: {type: string}
        country: {type: string}
        postalCode: {type: string}
